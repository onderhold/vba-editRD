{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Move to dev-personal",
            "type": "shell",
            "command": "${workspaceFolder}/.dev-personal/scripts/move-to-dev-personal.ps1",
            "args": [
                "${input:pathToMove}"
            ],
            "problemMatcher": [],
            "presentation": {
                "echo": true,
                "reveal": "always",
                "focus": true,
                "panel": "dedicated",
                "showReuseMessage": false,
                "clear": true
            },
            "options": {
                "env": {
                    "VIRTUAL_ENV": ""
                }
            }
        }
    ],
    "inputs": [
        {
            "id": "pathToMove",
            "type": "promptString",
            "description": "Path to move (relative to workspace root, e.g., 'docs/file.md' or 'build/')",
            "default": ""
        }
    ]
}
